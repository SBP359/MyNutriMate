import { defineConfig } from 'vite';

export default defineConfig({
  // Configuration has been simplified to remove any path aliases.
  // We now use standard relative paths for all module imports.
});
